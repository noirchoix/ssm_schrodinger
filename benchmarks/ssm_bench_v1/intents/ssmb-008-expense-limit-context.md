# Delegated Expense Approval
Build a FastAPI PostgreSQL expense system where an approval action checks claim.amount against approver.approval_limit. Keep that rule in workflow context, not generic CRUD.
