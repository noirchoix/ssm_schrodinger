# Inventory Memory Service
Build an in-memory FastAPI inventory API with JWT and Product CRUD. Product has name, sku, quantity, and max_stock. PATCH must preserve omitted fields and reject quantity above max_stock.
