# Contradictory Persistence Brief
Build a FastAPI inventory API with JWT and Product CRUD. The production requirement says PostgreSQL is mandatory. The same release must also use only in-memory persistence and must not connect to PostgreSQL.

## Entities
- Product: name, sku and quantity
